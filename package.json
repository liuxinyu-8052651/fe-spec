{
  "name": "lxy-fe-spec",
  "private": true,
  "description": "lxy--前端编码规范工程化",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "init": "pnpm install",
    "publish": "lerna publish",
    "clean": "lerna clean && rm -rf node_modules"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
